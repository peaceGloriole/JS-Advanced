function solve(name, population, treasury) {
  const cityParams = {
    name,
    population,
    treasury
  };

  return cityParams;
}

solve('Tortuga',
  7000,
  15000);
solve('Santo Domingo',
  12000,
  23500);